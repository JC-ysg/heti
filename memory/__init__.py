"""Hēti memory layer: the first loop (import → raw → index → query).

Layers:
- L0: plain markdown files in the vault (notes + raw/). Readable by eye with all code deleted.
- L1: the fields and rules (contract.FIELDS / TYPES, raw_store.RAW_FIELDS, gate checks).
- L2: this code (adapters, gate, index, CLI). Replaceable within a day.
"""
